# backend/.env.example

# Used to determine dev vs production logic in FastAPI
ENV_MODE=development

# Optional: Change the port or host if needed
HOST=0.0.0.0
PORT=8080

# Optional: Custom API version (shown on /api/version)
API_VERSION=1.0.0
